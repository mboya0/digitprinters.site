{
  "name": "digitprinters.site",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "digitprinters"
  ],
  "scripts": {
    "dev": "cd digitprinters && npm run dev",
    "build": "cd digitprinters && npm run build",
    "preview": "cd digitprinters && npm run preview",
    "lint": "cd digitprinters && npm run lint"
  }
}
