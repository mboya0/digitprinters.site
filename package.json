{
  "name": "digitprinters.site",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "digitprinters"
  ],
  "scripts": {
    "dev": "npm --prefix digitprinters run dev --",
    "build": "npm --prefix digitprinters run build --",
    "preview": "npm --prefix digitprinters run preview --",
    "lint": "npm --prefix digitprinters run lint --"
  }
}
